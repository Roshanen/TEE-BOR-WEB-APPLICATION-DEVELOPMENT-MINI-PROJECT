@model List<EventViewModel>
@{
    ViewData["Title"] = "Home Page";
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tee Bor</title>
    <link rel="stylesheet" href="~/css/index.css"></link>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>

<body>
    <div class="container">
        <div class="event-group event-ongoing">
            <div class="header">
                <h2>Ongoing Event</h2>
                <a href="#">See more</a>
            </div>
            <div class="event-container">
                @foreach (var ev in Model)
                {
                    <div class="event event-1">
                        <a href="/eventpage/viewid/@ev.Tags">
                            <div class="event-img-container">
                                <img class="event-image" src="@ev.EventImg" alt="event-1 image">
                            </div>
                        </a>
                        <div class="content">
                            <div class="party-name"><a href="/eventpage/viewid/@ev.Tags" class="event-name">@ev.EventName</a></div>
                            <div class="host">Host: @ev.HostName</div>
                            <div class="date-hide">
                                <div><span class="material-symbols-outlined">calendar_month</span>Start at: @ev.EventStartDate</div>
                            </div>
                            <div class="detail"><p>Details:</p> @ev.EventDetails</div>
                            <div class="event-footer">
                                <div class="going">
                                    <span class="material-symbols-outlined">check_circle</span>
                                    <div>1 Going</div>
                                </div>
                                <div class="price">
                                    <span class="material-symbols-outlined">confirmation_number</span>
                                    <div>Free</div>
                                </div>
                            </div>
                            <div class="date">
                                <span class="material-symbols-outlined">calendar_month</span>
                                <div>Start at: @ev.EventStartDate</div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
    @if (ViewData["userID"] != null){
    <a href="/event/create" class="link-create">
        <div class="div-create">Create Event</div>
    </a>
    }
</body>

</html>
